The simulator uses Node worker threads and is not exported from the engine's
main barrel (browsers bundle that). Import it by path:
`@dangerous-inclinations/engine/dist/sim/index.js` or run `yarn sim`.
